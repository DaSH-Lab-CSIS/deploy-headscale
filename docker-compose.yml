services:
  headscale:
    image: headscale/headscale:0.25.0
    restart: unless-stopped
    container_name: headscale
    ports:
      - "8080:8080"
      - "9090:9090"
    volumes:
      - ./config:/etc/headscale
    command: serve
